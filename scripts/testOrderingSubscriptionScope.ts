/** Retired name retained for anyone with an old local command. */
console.error(
  "testOrderingSubscriptionScope is retired. Run npm run test:ordering-profile-access instead.",
);
process.exitCode = 1;
