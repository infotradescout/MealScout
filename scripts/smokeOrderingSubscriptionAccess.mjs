/** Retired name retained for anyone with an old local command. */
console.error(
  "smokeOrderingSubscriptionAccess is retired. Run npm run smoke:ordering-profile-access instead.",
);
process.exitCode = 1;
