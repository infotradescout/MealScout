# Isolated recovery proof runner

`scripts/verifyRecoveryIsolated.mjs` runs only in an explicitly selected non-root Linux x64 proof build. It requires a clean checkout at `RENDER_GIT_COMMIT`, rejects ambient database/payment/mail credentials and loadable environment files, and passes a narrow tooling environment to child processes. The two tracked `.env.*.example` templates are inert; no application server is started.

The runner installs the canonical npm lockfile, checks types, builds the application and executes the existing calendar, critical smoke, ordering, integrated marketplace, webhook and menu/LISA tests. It builds the complete upstream PostgreSQL16.14 release into an owned temporary prefix outside the checkout, using the SHA256 published alongside the [official source archive](https://ftp.postgresql.org/pub/source/v16.14/). The existing migration142 proof owns its first native cluster. The new transport adapter then owns a separate fresh cluster, pushes the canonical schema, applies the same migrations090/142 as the existing Docker orchestrator, and runs that existing stateful fixture unchanged. Payments and notifications in this fixture use intercepted providers and synthetic data. Both fixtures stop their own clusters before returning.

The existing nine credential-free Playwright files run on all five configured desktop/mobile projects against a loopback static server serving the production build. `FRONTEND_URL` prevents Playwright from starting an application server. Missing API mocks receive HTTP404; there is no backend or external database. Browser failures, flaky results, skips, zero executed tests or a changed checkout fail the proof. The runner publishes only its summary, exact SHA/tree, executed step outcomes and browser counts. A failed command exits nonzero and prints the failure summary to build logs; a failed deployment must never be described as successful proof.

Deployment configuration is a separate static-only Render service with automatic deploys disabled, no paid plan, database, environment group, production domain or provider credentials. Its build makes a fresh temporary checkout of the selected commit and copies only `test-results/recovery-isolated-report` into the static publish directory after a successful run. This runner does not call production startup, migration deployment, live-provider smoke commands or authenticated customer workflows.

Local preparation: both JavaScript files pass `node --check`; the explicit-mode guard was executed and failed before any tests. Native PostgreSQL/browser acceptance remains unexecuted locally because this workspace runs as root and cannot launch the required native runtimes. Hosted acceptance must be reported from its actual run and commit, independently of historical evidence. Passing this scope does not establish live Stripe behavior or complete production readiness.

The first hosted attempt at537d0ed7 passed fresh install, typecheck, production build and all selected safety/menu suites. It correctly stopped before native database startup: the initially selected npm server package omitted `psql`, `pg_dump` and `pg_restore`. Source compilation supplies those actual required tools; no fixture assertion or acceptance gate was removed. That failed attempt does not count as native or browser acceptance.

At d04bbde1, native migration142 apply/replay/containment/concurrency/restore and the complete intercepted-provider stateful fixture passed; both owned clusters stopped. The browser matrix executed170 cases:102 passed,68 Safari cases could not launch because the host lacked WebKit libraries. The library adapter now downloads the observed Debian12 dependencies through a private APT configuration and extracts them into the owned temporary prefix. It uses an owned copy of package status, owned indexes/cache/logs, explicitly signed official Debian sources, and download-only mode; no system packages or host APT hooks are installed or run. Only the browser subprocess receives those library paths. The report directory is recreated after Playwright clears its output directory, preserving a truthful failure receipt as well as a successful one. The full unchanged170-case matrix remains mandatory.

At fa2c5c60, library retrieval/extraction and the unchanged-system-status assertion passed; seven missing library checks resolved. The remaining Safari prerequisite failure was `libGLESv2.so.2`: Playwright1.57 checks its presence through `/sbin/ldconfig -p`, which cannot see the owned `LD_LIBRARY_PATH`. The prerequisite adapter explicitly executes the canonical checks for Chromium, Chromium headless, Firefox and WebKit. It rejects every error except that exact sole WebKit cache miss, then compiles and executes a native `dlopen`/`glGetString` probe under the same library environment. Only after those checks pass does it use Playwright's supported host-validation override for the browser subprocess. No browser launch, case, assertion, failure, flake or skip is accepted by this adaptation; all170 real cases still must pass. System libraries and the system loader cache remain unchanged.

At1bfe6c4d, the canonical prerequisite checks and actual GLES loader probe passed, but68 Safari cases stopped at launch: WebKit's bundled MiniBrowser wrapper replaces `LD_LIBRARY_PATH` with its own library directories, hiding the extracted libraries. The wrapper behavior is defined by the [pinned upstream bundle generator](https://github.com/WebKit/WebKit/blob/486de399887bc8fa8a69e2f194ebc9476589a08a/Tools/Scripts/webkitpy/binary_bundling/bundle.py). The adapter now links only missing extracted library names into the two owned WebKit bundle library directories. It verifies source and destination real paths remain inside the disposable tooling directory, never replaces existing libraries, and records each bundle's link count. Browser binaries and launchers remain unchanged. Acceptance still requires the actual canonical checks, loader probe, all170 browser cases and clean exact source.
