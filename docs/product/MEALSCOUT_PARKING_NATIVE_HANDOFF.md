# MealScout Parking Pass — production cutover completed September 21

## Scope and resume point
MealScout only. Preserve `docs/product/MEALSCOUT_END_TO_END_FLOW_UX_MATRIX.md` and all existing feature/UI work. The owner-waived billing check is not a waiting gate and was not relabeled as passed. Desktop Commander is not a dependency; reuse the existing executor and do not add another proof service.

**PR383 and PR380 are merged. The booking-route release and canonical migration142 are now deployed.** Actual runtime source: `7d51a8a49195616152583f99c5fac2d2bfe2532d`, tree `e56996af1317bd4b1fd58a4a0e874431e76d2881`. Production Render service `srv-d5escdh5pdvs73foo41g`, deploy `dep-daomu68jo6nc73cqtojg`, live at `2026-09-21T17:57:59.439296Z`, instance `srv-d5escdh5pdvs73foo41g-v92h9`. This handoff/receipt commit is documentation only, not the runtime SHA.

**Current receipt:** `docs/qa/mealscout-parking-production-cutover-2026-09-21.json`. Native candidate evidence stays at `docs/qa/mealscout-parking-integrated-release-proof-2026-09-21.json`; do not relabel its source215b6868 as an execution of later documentation or deployment commits.

## Completed staged cutover
Phase A source102f054b copied the verified application/configuration unchanged, with migrations identical to former productionc10700e3 and no142. Its actual-route compatibility test passed20/20 against the retained old all-history guard: rebooking was temporarily denied409 with terminal history unchanged and no provider operation; the other nineteen capacity/eligibility/recovery assertions were retained. The test harness was explicitly adapted, not the application. Its executor source56f5633c and deploymentdep-daomnkp42hec738snss0 are bound in the receipt. A preceding checkout-only harness failure is retained separately.

PR383 merged phase A as `ef096d39ead2f0be85e895b0505f7bab019ca196`, deployment `dep-daomoklg1s2s73arf4n0`, live17:44:16.959277Z. The old deploy was deactivated. Old instancezhltr memory samples ended17:45; only replacementlg29d reported17:46–17:51, with instance_count1. The conservative ordinary documented shutdown bound (60seconds beforeSIGTERM plus up to300seconds delay) elapsed17:50:16.959277Z. This is platform lifecycle/telemetry evidence, not an OS process-list or captured kill receipt; no extended support-configured delay is known.

Phase B was merged at17:53:25Z, after that retirement evidence. Two-parent integrationde2e774d preserves both branch histories and the phase-A policy document. The ancestor comparison against phase A has only reviewed142, provider/probe/test scripts and the build hook; no client/server/shared runtime change. The canonical `npm run migrate:deploy` logged applying142 at17:56:57.164553308Z and **PASS (1 applied,23 already recorded)** at17:56:57.252276017Z. Historical bootstrap was not replayed. No migration ledger entry or billing/protection setting was manipulated.

## Exact public-runtime observation
The existing proof executor ran a read-only probe at17:59:37.398Z–17:59:40.525Z, deploy `dep-daon0ig473hc73d1tigg`, executor sourcede2e774d. Both `mealscout.onrender.com` and `www.mealscout.us` returned the exact production7d51a8a4 through `/api/version`, withRENDER_GIT_COMMIT and a present backend frontend-asset directory. `/health/ready` returned200/dbok; parking listings returned200/376entries; host-ids returned200; unauthenticated hosts returned401. Payment configuration returned200/live mode. Aggregate payment-health counts were zero at the observation. These are read-only backend observations, not independent verification of every Vercel asset or a live customer payment.

## Connected-provider evidence and honest remaining scope
The production build used existing service credentials for actual Stripe GET requests. Its source-bound receipt at17:54:17.791714044Z verifies live account authentication, charges_enabled, payouts_enabled, and one enabled matching webhook endpoint configured forpayment_intent.succeeded. No key values were logged. Eight safety tests passed both in the isolated executor and the production build: live credentials cannot trigger synthetic writes; explicit sandbox execution is unconfirmed and cannot charge/capture/refund.

**Provider acceptance remains partial:** no sandbox credential was present, so no PaymentIntent was created. Remote webhook delivery, completed customer checkout and settlement remain unproved. Do not say an endpoint configuration or aggregate zero proves a payment. No manual customer account/booking, live payment/refund or message was created. A full production booking-row snapshot comparison and independently restorable backup were not verified; no such receipt is claimed.

Next actual work is an authorized real owner/customer booking acceptance with webhook reconciliation (or sandbox execution once a test credential is available), plus any separate production backup/restore assurance. Do not restart native20/type/build/migration rehearsal or reopen the completed cutover merely because documentation HEAD differs. Never use a live credential for a synthetic payment test or invent missing access.

## Preserved implementation and prior proof
The provider-create error catch repaire86fcbd7 leaves reservations and concurrently paid state unchanged, returns503 `booking_request_unresolved`, and preserves the original checkpoint for positive-evidence same-reference recovery. The native20cases prove capacity1/16trucks, capacity3/12trucks, eligibility denials, revoked replay, retries/restarts/process death, returned errors/disconnections, unavailable search, paid/cancelled history, full-date rollback and multi-day hold retention without another provider create.

Migration142 SHA256`6fb140adf20d8621a2f99039675834d2200c535267090327626e81ce9cb1e701` validates active replacement protection and both known legacy guards before removal. Eleven native guard scenarios pass; the complete148SQL rehearsal retains278foreign keys, cancelled+active coexistence, active-duplicate rejection and unchanged rerun ledgers/history. Native candidate215b6868 passed11release stages; raw receipt SHA256`4d2601d5e801bf89182749a740cf89eb8884bdb31269e59e2e7435075e8317a2`. The route/provider boundary fixtures and separate full-schema rehearsal remain explicitly distinct. Older UI/browser/Orders/map receipts and the25-case native receipt keep their own source and scope.

Retain the original paid-hold, provider-response, missing-extension and full-schema-history failure receipts. Their application failures were repaired, not erased or waived. This continuation used the existing hosted executor only; owned native compatibility clusters/directories were stopped/removed, and the final public probe performedGETonly. Production configuration was not changed; two application deploys and one canonical schema migration were the rollout side effects.
